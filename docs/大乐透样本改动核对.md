# 大乐透样本改动核对文档

对比对象:
- **原版**:`lottery.py` 的 `daletou()` 输出(103 列)
- **改版**:`sample_output/大乐透转化结果_样本改.xlsx`(122 列)

下面逐项列出我从你的改版里识别到的改动,以及需要你补充确认的地方。请逐条核对,标注对/错/补充。

---

## 一、列结构总览

| | 原版 | 改版 |
|---|---|---|
| 总列数 | 103 | 122 |
| 净增 | — | +19 列 |

改版整体新增了一大块"蓝球(前区)的奇偶/大小"统计,以及把后区相关列重构。

---

## 二、确认到的改动逐项

### 1. 加粗分组竖线(待全表应用)✅ 已理解需求
- 你的需求:**按第一行表头的分组**,在每个分组的左右两侧加粗竖线,方便用第一层名称区分不同区块(例:`蓝1` 左右两边加粗)。
- 改版现状:目前只在 **`蓝1` 组(五行1/相克1,E/F 两列)** 做了加粗示例(左边 thick、右边 thick)。
- **我的实现计划**:遍历第一层表头,在每个分组的**第一列左边框**和**最后一列右边框**统一加粗(thick)。全表所有分组(蓝1~蓝5、蓝球统计、奇偶、蓝球1/2/3区、红球、红球分布等)都套用。
- ❓ 待确认:加粗只要**竖线**,横线维持原来的细线,对吗?

### 2. 新增「斜号」列 ✅ 已识别
- 位置:`蓝球统计` 组内,顺序为 `…重号 | 斜号 | 连号 | 同位`(原版是 `连号 | 重号 | 同位`,没有斜号)。
- 含义(按你描述):本期号码与**上一期**号码相比,是否存在"斜着相邻"的数 —— 即本期某号码 n,上期出现了 n-1 或 n+1,则计一个斜号。例:本期 27,上期有 26、28,则贡献 **2** 个斜号。
- 改版数据校验:重号/斜号/连号 前几行数值相同(都是 0,1,0,0,1),样本数据巧合,逻辑需我按定义实现。
- ❓ 待确认(关键):
  - (a) 斜号是**统计"本期有多少个号码满足'上期存在它的±1'"**,还是**统计上期里所有±1命中的总次数**?
    - 例:本期 27,上期同时有 26 和 28 → 算 **1**(27 这个号满足条件)还是算 **2**(26、28 各算一次)?你描述里说"有两个斜号",倾向**按命中的上期号码个数计数(=2)**,请确认。
  - (b) 只跟**上一期**比,对吧?(不是跟前 N 期)

### 3. 新增前区「奇偶」与「大小」分列 ✅ 已识别
原版只有整体 `奇偶/奇数、偶数` 两列。改版新增了**按每个前区球位**的奇偶和大小:
- `奇偶/奇 | 偶`(整体,保留)
- `蓝1/奇 | 偶`、`蓝2/奇 | 偶` … `蓝5/奇 | 偶`(每球位的奇偶,共 10 列)
- `蓝1/大 | 小`、`蓝2/大 | 小` … `蓝5/大 | 小`(每球位的大小,共 10 列)
- ❓ 待确认:
  - "奇/偶"这两列,对每个球位来说是**互斥的 0/1 标记**(奇数则"奇"列=1、"偶"列空)吗?
  - "大/小"的分界线是多少?(常见是前区 1-17 为"小"、18-35 为"大",或按中位数。请给出确切阈值)

### 4. 后区「红1/红2」改为「后区号码 A+B」✅ 已识别
- 原版:`红球/红1`、`红球/红2` 两列分别放后区两个号码。
- 改版:合并为单列 **`红球/后区号码`**,用 `A+B` 形式(如 `02+04`、`05+11`)。
- ✅ 已确认,会按 `A+B`(两位补零)输出。

### 5. 红球分布末尾「一区/二区」替换原「红球统计」三列 ✅ 已识别
- 原版结尾是 `红球统计/红连号、红重号、红同位` 三列。
- 改版结尾是 `红球分布/一区、二区` 两列。
  - **一区**:后区号码落在 **0–9** 的个数
  - **二区**:后区号码落在 **其他(10–12)** 的个数
- ❓ 待确认:
  - (a) 原来的 `红连号/红重号/红同位` 三列是**整列删除**,对吗?
  - (b) 一区是 1–9 还是 0–9?(后区号码范围是 1–12,实际等价,但确认下边界)
  - (c) 改版数据里这两行示例都是 `0/0`,看起来没填值 —— 逻辑以我上面的理解为准,对吗?

### 6. 后区号码着色(❗需你补充)
- 你的需求:后区号码 **红1~红9 一个颜色、红10~红12 另一个颜色**。
- 改版现状:**我在前两行的红球分布列(红1-红12)没有检测到任何字体色或填充色**。可能是:着色没保存上,或你想表达的是给「后区号码(A+B)」那一列按号码值上色。
- ❓ 待确认(关键,缺这个我没法实现):
  - (a) 着色对象是哪个?是 **红球分布里命中的那个格子**(如后区出了 4,就给"红4"格上色),还是 **"后区号码 A+B"那一列的文字**?
  - (b) 两个颜色分别用什么?请给具体色值或描述(如:1-9 用浅橙、10-12 用浅紫)。
  - (c) 是**填充背景色**还是**字体颜色**?

---

## 三、其他我注意到的细节(请你确认是否有意为之)

### 7. 表头分组名沿用"蓝/红"反命名
- 改版和原版一样,前区 5 球的列归在 `蓝1`~`蓝5`、`蓝球1/2/3区` 下,后区归在 `红球`/`红1`/`红2` 下 —— 这个"蓝实为前区、红实为后区"的反命名,你是**保留**还是要我**改正**成"前区/后区"?

### 8. 蓝球统计内列顺序变化
- 原版:`1-9 | 10-19 | 20-29 | 30-35 | 连号 | 重号 | 同位`
- 改版:`1-9 | 10-19 | 20-29 | 30-35 | 重号 | 斜号 | 连号 | 同位`
- 即:把"连号、重号"换序为"重号、斜号、连号",并插入斜号。确认按改版顺序。

### 9. 表头加粗
- 改版表头(第 2 行)字体为**加粗**。原版表头不加粗。确认表头整体加粗。

---

## 四、最终定稿（已与你确认）

1. **斜号计数口径**:**算 2** —— 统计上期里所有命中本期某号 ±1 的号码个数(本期27、上期有26和28 → 2)。只跟**上一期**比。
2. **大/小阈值**:按**个位数** —— 个位 0–4 为"小",5–9 为"大"。(每球位的"大""小"为互斥 0/1 标记)
3. **后区号码着色**:对**后区红球分布命中的格子**着背景色。
   - 大乐透:红1–红9 一色、红10–红12 另一色
   - 双色球:1–9 一色、10–16 另一色
   - 具体色值由我选取(区分明显的浅色),做出来后你看效果再微调。
4. **一区边界**:**0–9**(二区为其余)。
5. **红连号/红重号/红同位**:**整列删除**。
6. **蓝/红反命名**:**保持现状**(继续用蓝1~蓝5、红球分布等)。
7. **加粗竖线**:每个一级分组的**左右边界加粗竖线**,横线保持细线。表头整体加粗。

### 双色球套用差异(已确认)
- 双色球 = 6 红球(1–33) + 1 蓝球(1–16)。整套改动同样套用。
- 后区只有 1 个球:**"后区号码"列直接显示单个号码**(不用 A+B)。
- 双色球后区"一区/二区"与着色:**1–9 一区/一色,10–16 二区/另一色**。
- 前区奇偶/大小分列同样按 6 个球位展开。
